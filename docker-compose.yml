services:
  azulweb:
    build: .
    image: azulweb
    ports:
      - "8000:8000"
    volumes:
      - ${WORKSPACE:-./data}:/app/workspace
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped