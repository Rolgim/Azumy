services:
  azulweb:
    build: .
    image: azulweb
    ports:
      - "8000:8000"
    volumes:
      - ${WORKSPACE:-./data}:/app/workspace
      - ./secrets/netrc:/root/.netrc:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped